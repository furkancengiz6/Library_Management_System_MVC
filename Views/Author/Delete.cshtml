@model Author


<div class="container mt-5">
    <div class="card shadow-lg">
        <div class="card-header bg-danger text-white">
            <h3><i class="bi bi-exclamation-triangle-fill"></i> Yazar Sil</h3>
        </div>
        <div class="card-body">
            <p class="fs-5"> "<strong>@Model.FirstName @Model.LastName</strong>" yazarını silmek istediğinize emin misiniz?</p>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />

               
                <div class="mb-3">
                    <label class="form-label">Ad Soyad</label>
                    <input asp-for="FirstName" class="form-control" disabled />
                    <input asp-for="LastName" class="form-control" disabled />
                </div>

              
                <div class="mb-3">
                    <label class="form-label">Doğum Tarihi</label>
                    <input asp-for="DateOfBirth" type="date" class="form-control" disabled />
                </div>

                
                <div class="d-flex justify-content-center gap-3">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Sil
                    </button>
                    <a asp-controller="Author" asp-action="List" class="btn btn-dark">
                        <i class="bi bi-arrow-left"></i> İptal
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@*
<h2>Yazar Sil</h2>

<p>Yazarı silmek istediğinize emin misiniz?</p>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />
    <div>
        <label>Ad Soyad</label>
        <input asp-for="FirstName" class="form-control" disabled />
        <input asp-for="LastName" class="form-control" disabled />
    </div>
    <div>
        <label>Doğum Tarihi</label>
        <input asp-for="DateOfBirth" type="date" class="form-control" disabled />
    </div>
    <button type="submit" class="btn btn-danger">Sil</button>
    <a asp-controller="Author" asp-action="List" class="btn btn-primary">Geri Dön</a>
</form>
*@